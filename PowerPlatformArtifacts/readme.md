These are the artifacts required to register the National Grid GIS Enhanced V10 connector with Power Platform APIM.

- `apiDefinition.swagger.json` - OpenAPI definition of the connector.
- `apiProperties.json` - connection parameters (on-premises data gateway) and connector policies.
- `settings.json` - `paconn` settings pointing at the existing V10 connector in the target environment.
